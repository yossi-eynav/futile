const shuffle = require('./shuffle');
const pluck = require('./pluck');

/**
 * Array helpers
 * @exports arr
 * @description A collection of array helpers
 *
 * @requires arr/shuffle
 * @requires arr/pluck
 */
module.exports = {
    shuffle,
    pluck
};
