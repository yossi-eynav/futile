const interpolate = require('./interpolate');
const resolve = require('./resolve');

/**
 * String helpers
 * @module str
 * @since 0.0.1
 * @description A collection of string helpers
 *
 * @requires str/interpolate
 * @requires str/resolve
 */
module.exports = {
    interpolate,
    resolve
};
