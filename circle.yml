machine:
  node:
    version: 6.9.4

test:
  override:
    - npm run lint
    - npm test

general:
  branches:
    ignore:
      - gh-pages

experimental:
  notify:
    branches:
      only:
        - master
