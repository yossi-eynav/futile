machine:
  node:
    version: 6.9.4

dependencies:
  pre:
    - sudo apt-get install nodejs
    - npm install
test:
  override:
    - npm run lint
    - npm test

experimental:
  notify:
    branches:
      only:
        - master
